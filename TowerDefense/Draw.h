#pragma once
#include "Controller.h"

using namespace std;

class Draw
{
	public:
//Nam
        // Old
        static void drawBlock(SHORT, SHORT, SHORT, SHORT, int);

        static void drawA(SHORT, SHORT, SHORT, SHORT, int);
        static void drawB(SHORT, SHORT, SHORT, SHORT, int);
        static void drawC(SHORT, SHORT, SHORT, SHORT, int);

        static void drawD(SHORT, SHORT, SHORT, SHORT, int);
        static void drawE(SHORT, SHORT, SHORT, SHORT, int);
        static void drawF(SHORT, SHORT, SHORT, SHORT, int);

        static void drawG(SHORT, SHORT, SHORT, SHORT, int);
        static void drawH(SHORT, SHORT, SHORT, SHORT, int);
        static void drawI(SHORT, SHORT, SHORT, SHORT, int);

        static void drawJ(SHORT, SHORT, SHORT, SHORT, int);
        static void drawK(SHORT, SHORT, SHORT, SHORT, int);
        static void drawL(SHORT, SHORT, SHORT, SHORT, int);

        static void drawM(SHORT, SHORT, SHORT, SHORT, int);
        static void drawN(SHORT, SHORT, SHORT, SHORT, int);
        static void drawO(SHORT, SHORT, SHORT, SHORT, int);

        static void drawP(SHORT, SHORT, SHORT, SHORT, int);
        static void drawQ(SHORT, SHORT, SHORT, SHORT, int);
        static void drawR(SHORT, SHORT, SHORT, SHORT, int);

        static void drawS(SHORT, SHORT, SHORT, SHORT, int);
        static void drawT(SHORT, SHORT, SHORT, SHORT, int);
        static void drawU(SHORT, SHORT, SHORT, SHORT, int);

        static void drawV(SHORT, SHORT, SHORT, SHORT, int);
        static void drawW(SHORT, SHORT, SHORT, SHORT, int);
        static void drawX(SHORT, SHORT, SHORT, SHORT, int);

        static void drawY(SHORT, SHORT, SHORT, SHORT, int);
        static void drawZ(SHORT, SHORT, SHORT, SHORT, int);


        static void drawSettingIcon(SHORT, SHORT, SHORT, SHORT);
        static void drawHeartIcon(SHORT, SHORT, SHORT, SHORT);
        static void drawCoinIcon(SHORT, SHORT, SHORT, SHORT);
        static void drawPineIcon(SHORT, SHORT, SHORT, SHORT);

        // New
        static void drawFrame(SHORT, SHORT, SHORT, SHORT,int ,int, int, int);

        static void printConsoleInput(int, SHORT, SHORT, SHORT, SHORT, int, int);

        static void drawColon(SHORT, SHORT, SHORT, SHORT, int); 

// Khanh
        

        static void drawSantaClaus(SHORT, SHORT, SHORT, SHORT);

        static void drawZero(SHORT, SHORT, SHORT, SHORT, int);
        static void drawOne(SHORT, SHORT, SHORT, SHORT, int);
        static void drawTwo(SHORT, SHORT, SHORT, SHORT, int);
        static void drawThree(SHORT, SHORT, SHORT, SHORT, int);
        static void drawFour(SHORT, SHORT, SHORT, SHORT, int);
        static void drawFive(SHORT, SHORT, SHORT, SHORT, int);
        static void drawSix(SHORT, SHORT, SHORT, SHORT, int);
        static void drawSeven(SHORT, SHORT, SHORT, SHORT, int);
        static void drawEight(SHORT, SHORT, SHORT, SHORT, int);
        static void drawNine(SHORT, SHORT, SHORT, SHORT, int);

        static void drawIcon(SHORT, SHORT, SHORT, SHORT, int, int);
        static void drawIconPlay(SHORT, SHORT, SHORT, SHORT, int);
        static void drawIconLeaderBoard(SHORT, SHORT, SHORT, SHORT, int);
        static void drawIconExit(SHORT, SHORT, SHORT, SHORT, int);
        static void drawIconSave(SHORT, SHORT, SHORT, SHORT, int);
        static void drawIconMusic(SHORT, SHORT, SHORT, SHORT, int);
        static void drawIconHelp(SHORT, SHORT, SHORT, SHORT, int);
};
